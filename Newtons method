#include<stdio.h>
#include<math.h>

float f(float x){

    return x*log(x)-1.2;
    //return (x*x)-4*x-7;
}
float fp(float x)
{
    return log(x)+1;
   //return (2*x)-4;
}

int main()
{
    float x=5,x2;
    int i=0;
   do{

       x2=x-(f(x)/fp(x));
       printf("In iteration %d value is : %f\n",i,x2);
       int e=x-x2;
       x=x2;
       i++;
       if(e>0.0001||e<-0.0001){
           break;
       }

    }while(1);
printf("The value is : %f \n",x);

}
